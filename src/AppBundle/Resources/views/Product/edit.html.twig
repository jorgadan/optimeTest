{% extends 'base.html.twig' %}

    {% block body %}
        <div class="module">
            <div class="module-head">
                <h3>Editar Producto
                    <a onclick="window.history.back()" class="btn btn-info pull-right">Volver</a></h3>
            </div>
            <div class="module-body">
            {% trans_default_domain 'messages' %}
            {% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}
            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
            {{ form_end(edit_form) }}

            <div class="row">
                <a onclick="deleteProduct()" class="btn btn-warning pull-right">Borrar</a>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script>
        function deleteProduct() {
            if (confirm('Está seguro de borrar el producto?')) {
                window.location = "{{ path('product_delete', {'id': product.id}) }}";
            }
        }
    </script>
{% endblock %}
